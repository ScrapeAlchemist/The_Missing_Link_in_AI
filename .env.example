# Bright Data API Configuration
# Get these from: https://brightdata.com/cp

# ===== REQUIRED =====
# API Key from Settings → API tokens
# This key is used for both API access and MCP integration
BRIGHTDATA_API_KEY=your_api_key_here

# ===== OPTIONAL =====
# OpenAI API key for LLM-powered synthesis (recommended for best results)
# If not set, the workflow will use a heuristic summarizer with citations
# Get your key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Optional model override (default: gpt-4o-mini)
OPENAI_MODEL=gpt-4o-mini

# ===== OPTIONAL (Only for demo scripts) =====
# Zone names from Zones page - only needed for npm run demo:api and npm run demo:mcp
# The main workflow (npm start) does NOT require these
SERP_ZONE=your_serp_zone_name
UNLOCKER_ZONE=your_unlocker_zone_name
